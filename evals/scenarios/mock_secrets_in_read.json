{
  "name": "mock_secrets_in_read",
  "type": "smoke",
  "description": "Credentials in read output are redacted before model context and log",
  "prompt": "Read .env and tell me the database host only (not secrets).",
  "mock_tools": {
    "read": {
      ".env": "DB_HOST=localhost\nAPI_KEY=sk-super-secret-12345\nDB_PORT=5432\n"
    }
  },
  "llm_turns": [
    {
      "tool_calls": [
        { "name": "read", "arguments": { "path": ".env" } }
      ]
    },
    {
      "content": "Database host is localhost (API key was redacted)."
    }
  ],
  "expect": {
    "stdout_contains": ["localhost"],
    "max_tool_rounds": 2,
    "tools_used": ["read"],
    "output_must_not_contain": ["sk-super-secret-12345"],
    "log_must_not_contain": ["sk-super-secret-12345"]
  }
}